# Moldova Drought Intelligence Dashboard — dependencies
# Install with: pip install -r requirements.txt
#
# Versions are pinned to the tested set so deployments (e.g. Streamlit
# Community Cloud) reproduce the verified app exactly. scikit-learn in
# particular must match the version that trained the models/*.pkl artefacts.

streamlit==1.50.0
pydeck==0.9.1
plotly==6.7.0
pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
joblib==1.5.3

# Model training / data regeneration (train_model.py, generate_dashboard_data.py)
imbalanced-learn==0.12.4
